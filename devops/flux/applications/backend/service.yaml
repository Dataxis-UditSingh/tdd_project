apiVersion: v1
kind: Service
metadata:
  name: tdd-backend
  namespace: tdd-backend

spec:
  selector:
    app: tdd-backend

  ports:
    - name: http
      port: 3000
      targetPort: 3000

  type: ClusterIP