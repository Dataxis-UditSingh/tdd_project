apiVersion: source.toolkit.fluxcd.io/v1
kind: GitRepository
metadata:
  name: tdd-project
  namespace: flux-system

spec:
  interval: 1m

  url: https://github.com/Dataxis-UditSingh/tdd_project.git

  ref:
    branch: main

  secretRef:
    name: flux-system