apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: tdd-project
  namespace: flux-system

spec:
  interval: 1m

  path: ./devops/flux/applications

  prune: true

  sourceRef:
    kind: GitRepository
    name: tdd-project

  wait: true